#include<bits/stdc++.h>

using namespace std;

typedef long long int ll;

typedef unsigned long long int llu;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    while(t--)
    {
        int no_of_people,no_of_drinks;
        cin>>no_of_people>>no_of_drinks;
        vector<int> drinks(no_of_drinks);
        for(auto &drink:drinks) cin>>drink;
        vector<int> preferred_drink(no_of_people);
        for(int i=0;i<no_of_people;i++)
        {

        }
    }
    return 0;
}