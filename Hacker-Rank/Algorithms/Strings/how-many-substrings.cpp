#include<bits/stdc++.h>
using namespace std;
int main(int argc, char const *argv[])
{
  int n,t;
  cin>>n>>t;
  string s;
  cin>>s;
  vector<int> v(n+1);
  set<string> myset;
  v[0]=0;
  for(i=0;s[i]!=0;i++)
  {
    auto it=myset.begin();
    for(;it!=myset.end();it++)
    {
      insert
    }
    myset.insert(str())
    v[i+1]=v[i]+myset.count();
  }
  return 0;
}
