#include <bits/stdc++.h>

using namespace std;

void fillBoard(int m, int n, int x, int y)
{
    int a[m][n],i,j,xbeg,xend,begcar=x,endcar=0,flag=1;
    for(i=0;i<m;i++)
    {
      xbeg=begcar;
      xend=n-endcar;
      for(j=xbeg;j<xend;j++)
      {
        if(a[i][j])
      }
    }
}


int main()
{
    int m,n,x,y;
    cin>>m>>n>>x>>y;
    fillBoard(m,n,x,y);
    return 0;
}
