#include<bits/stdc++.h>

using namespace std;


int main(int argc, char const *argv[])
{
    int n;
    cin>>n;
    vector<int> v(n),v1;
    for(int i=0;i<n;i++) cin>>v[i];
    v1=v;
    sort(v1.begin(),v1.end());
    vector<pair<int,int>>
    for(int i = 0; i < n; i++)
    {
        
    }
    
    return 0;
}
